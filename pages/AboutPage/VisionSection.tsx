import React from "react";

// Placeholder component to satisfy Next.js legacy pages scanning.
// This file is not used in routing but avoids missing-module errors during build.
const VisionSection = () => {
  return null;
};

export default VisionSection;
